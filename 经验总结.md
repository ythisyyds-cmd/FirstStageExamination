# 第一阶段考核经验总结

第一阶段给出的学习路线是先接触Python、NumPy和PyTorch，再去看后面的深度学习课程。我实际学下来感觉中间还是缺少一大段练习的过渡。我大一上过C和C++的课，所以对Python里一些基本的逻辑不陌生，Python我主要是跟着黑马程序员的课程边学边敲代码，再用推荐的文档补充，感觉还比较顺利。但是NumPy、PyTorch还有课程中用到的Matplotlib，我没有找到比较侧重写具体代码的教程，感觉记住的东西不多，用到的时候还是会经常不知道该怎么写。

文档可以用来查询一个函数接收什么参数，也可以看到一些简单的示例，但是只看文档很难理解这些函数为什么要这样组合，以及有什么局限性。直接看后面推荐的刘二大人的课程感觉跨度比较大，前面的练习不够，课程中的代码很难自己复现，很多时候连具体卡在哪个概念上都说不清楚。所以我觉得光是把Python、NumPy和PyTorch的文档看一遍或者只运行里面提到的几个语句并不能完成这个过渡，还是需要自己动手写完整的代码。

我刚开始接触二分类时，光是为什么要用Sigmoid就花了一下午跟ai讨论。我当时想知道它的优势在哪里，什么样的函数可以替代它，为什么能够替代，以及二元交叉熵为什么要这样使用。Sigmoid的表达式本身并不难，但是从看懂表达式到明白它为什么适合放在这里，中间还有不少东西，而这些东西在课程里讲得比较模棱两可。这些问题我现在也不一定都能完整地搞懂讲明白，不过这个过程让我感觉比只记住y=wx+b，然后在后面接一个Sigmoid好得多，因为这让我对二分类的理解加深了。

第一项手动搭建BP网络是我感觉最困难的部分。原来见到的例子只有是和不是两种结果，MNIST却需要分辨0到9十个数字。我一开始会想还能不能继续使用比较熟悉的Sigmoid，也不清楚Softmax到底应该使用一次还是两次。后来才慢慢理解应该在我写的这个网络的输出层使用一次Softmax，把十个输出变成总和为1的概率，然后再用这些概率计算十分类交叉熵。

真正开始写以后，困难又从理解公式变成了怎样把想法落实成代码。怎样取一个batch的图片再把图片展开，张量之间怎样做矩阵运算，这些对我来说都很陌生。十分类交叉熵一开始也很难理解，因为程序只取出了真实数字对应的概率，再计算-log(p)，和我之前见过的二元交叉熵看起来差别很大。后面还要自己计算两个权重和两个偏置的梯度，再根据学习率分别更新。没有现成的自动求导和优化器以后，写起来麻烦很多。

这个任务我没有一次把整个程序写完，而是每次只增加一小部分能够运行的内容。先用DataLoader取出一个batch，打印图片和标签的形状，再完成图片展开和前向传播，检查隐藏层、输出层的形状以及每张图片的概率和。之后再单独写交叉熵和反向传播，先看损失能不能正常计算，再检查四个梯度的形状，最后才开始完整训练。这样做的过程比较慢，但是如果哪一步出现问题，至少不用从整个程序里盲找。

我在理解反向传播时也遇到不少困难，尤其是不明白把Softmax和交叉熵放在一起求导以后，为什么最后可以得到那么简单的结果。后来我还理解到Softmax和交叉熵放在一起后，可以先把输出的概率复制下来，再把真实类别对应的位置减1，得到的结果继续向隐藏层传递。这里中间还有求导的过程，并不是直接做一次减法就得到了结论。现在让我不看资料重新完成整段推导，我还是很难做到，但至少理解了代码中的这一步是从哪里来的。最后这个三层BP网络训练10轮后，在MNIST测试集上的准确率达到了94.30%。

第二项任务反而好一点，因为自动求导、交叉熵和优化器都可以直接使用，我能够把更多注意力放在网络的结构上，而且ResNet18的搭建也有比较明确的思路可以参考。这里比较麻烦的是设计和写代码时要把每一层的通道数和图片大小算准。如果有一个维度对不上，程序一般只会在进行某次计算时报错，很难直接反推出网络结构的哪一部分写错了。

我还是用了和第一项类似的办法，先测试一小部分再继续往后写。残差块先分别检查形状不变和形状发生变化的情况，确认输入输出都正确后，再把它们组成完整的网络。关于残差连接、1×1卷积、数据增强和实际训练中遇到的问题，我写在第二项任务的学习笔记里了。第二项虽然使用了更多封装好的功能，但只知道把函数放在一起是不够的，至少需要知道输入输出的形状怎样变化，不然报错以后还是不知道应该从哪里检查。

把这两个任务放在一起看，我对PyTorch的感觉也发生了一点变化。第一项把平时封装起来的前向传播、损失计算、反向传播和参数更新拆开了，写起来很困难，但是让我知道了loss.backward()和优化器大概做了什么。第二项又让我感受到这些封装确实很方便，可以把精力放到更大的网络结构规划上。我觉得手动实现在刚开始学习时做一次还是有用的，如果完全不了解里面的思路，就算会调用函数，也不知道它在做什么以及使用时需要注意什么。

我逐渐理解了网络的设计不是上来就能直接推导出哪一种最好，也不能提前安排好网络的哪一部分负责什么特征。更多时候是先尝试一种结构，再进行训练，根据训练和测试的结果判断好坏。我认为人更多时候不是直接设计智能，而是给它一个能够通过训练不断自我调整的环境。这个想法可能还不够严谨，但这是我做完两个任务以后比较明显的感受。

这也是我第一次用Git和GitHub记录项目。刚开始我不会保存、提交和推送，也遇到了邮箱和网络连接等问题。后来经过实操，我逐渐明白了什么时候适合提交，也知道怎样操作可以不上传本地的数据集和模型参数。虽然它和网络训练本身没有直接关系，但把每一阶段的代码留下来以后，可以看到自己这个项目是怎样一点点完成的，搞坏了也可以回滚。

如果让我重新学习这一阶段的内容，我不会先把几个文档从头到尾看完，也不会在前面的概念还比较模糊时就直接去复现完整的课程代码。我会先了解完成练习需要哪些知识，再边写边查文档，每次增加一部分可以运行和检查的代码。遇到公式时也不只记结论，如果中间过程比较复杂，可以先不全部展开，但是至少要知道这里省略了什么，不然到了手动实现时还是会卡住。

目前我对NumPy和PyTorch的很多写法仍然不熟悉，离开资料后经常写不出来。Matplotlib、TensorBoard和更复杂的训练方法也还暂时没有学，学习率、batch大小和通道数这些设置现在主要还是参考常见的数值来尝试。这次考核没有解决我学习中的所有问题，但是串联了一些原来比较零散的内容。现在我逐渐能从读取数据开始，跟着张量形状检查网络，再完成训练、测试、保存模型和预测图片，之后还需要通过更多练习慢慢补上中间缺少的部分。
