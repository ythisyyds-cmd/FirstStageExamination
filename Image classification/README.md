# CIFAR-10图像分类

使用手动搭建的ResNet18完成CIFAR-10数据集分类，没有使用预训练模型。

目前已经完成ResNet18网络结构、CIFAR-10数据读取、网络训练和测试，训练20轮后测试集准确率为82.99%。

运行train.py可以训练网络并保存模型参数。

把需要识别的图片放到当前项目文件夹中并命名为test_image.jpg，直接运行predict.py即可输出预测类别。